#include <bits/stdc++.h>
using namespace std;

int main()
{
	int i,max,min,a[5];
	for(i=0;i<5;i++)
	{
	    cin>>a[i];
	}
	max=a[0];
	for(i=0;i<5;i++)
	{
	    if(a[i]>max)
	    {
	        max=a[i];
	    }
	
	}
    min=a[0];
    for(i=1;i<5;i++)
    {
        if(a[i]<min)
        {
            min=a[i];
        }
    }
    cout<<"minimum value is"<<max<<"\n";
    cout<<"minimum value is"<<min;
}
